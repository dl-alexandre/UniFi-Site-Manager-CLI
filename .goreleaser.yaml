# GoReleaser configuration for UniFi Site Manager CLI

version: 2

project_name: usm

before:
  hooks:
    # Install dependencies
    - go mod tidy
    - go mod download
    # Run tests to ensure quality before release
    - go test ./...

builds:
  - id: usm
    main: ./cmd/usm
    binary: usm
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    goarm:
      - "7"
    ignore:
      # Skip 32-bit builds
      - goos: windows
        goarch: arm
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.gitCommit={{.Commit}}
      - -X main.buildTime={{.Date}}
    flags:
      - -trimpath

archives:
  - id: usm
    name_template: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}"
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"
  algorithm: blake3

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch
  groups:
    - title: Features
      regexp: "^feat[/(].*"
      order: 0
    - title: Bug Fixes
      regexp: "^fix[/(].*"
      order: 1
    - title: Improvements
      regexp: "^improve[/(].*"
      order: 2
    - title: Other
      order: 999

release:
  github:
    owner: dl-alexandre
    name: UniFi-Site-Manager-CLI
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
  header: |
    ## UniFi Site Manager CLI v{{.Version}}
    
    Full Changelog: https://github.com/dl-alexandre/UniFi-Site-Manager-CLI/compare/{{.PreviousTag}}...v{{.Version}}
  footer: |
    ## Installation
    
    ### Binary Download
    Download the appropriate binary for your platform from the assets below.
    
    ### Verification
    Verify checksums:
    ```bash
    b3sum -c checksums.txt
    ```

brews:
  - name: usm
    repository:
      owner: dl-alexandre
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
      branch: master
      pull_request:
        enabled: false
    commit_author:
      name: goreleaserbot
      email: bot@dl-alexandre.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    directory: Formula
    description: "Full-featured CLI for managing UniFi networks via Site Manager API"
    homepage: "https://github.com/dl-alexandre/UniFi-Site-Manager-CLI"
    license: "MIT"
    skip_upload: "{{ if index .Env \"HOMEBREW_TAP_TOKEN\" }}false{{ else }}true{{ end }}"
    test: |
      system "#{bin}/usm", "version"
    install: |
      bin.install "usm"

flatpak:
  - app_id: net.ddns.dl-alexandre.USMCLI
    runtime: org.freedesktop.Platform
    runtime_version: "24.08"
    sdk: org.freedesktop.Sdk
    finish_args:
      - --share=network
      - --filesystem=home

srpm:
  enabled: true
  summary: "Full-featured CLI for managing UniFi networks via Site Manager API"
  license: MIT
  url: https://github.com/dl-alexandre/UniFi-Site-Manager-CLI

# Requires SCOOP_BUCKET_TOKEN secret with 'repo' scope  
scoops:
  - repository:
      owner: dl-alexandre
      name: scoop-bucket
      token: "{{ .Env.SCOOP_BUCKET_TOKEN }}"
      branch: main
      pull_request:
        enabled: false
    commit_author:
      name: goreleaserbot
      email: bot@dl-alexandre.com
    commit_msg_template: "Scoop update for {{ .ProjectName }} version {{ .Tag }}"
    directory: bucket
    description: "Full-featured CLI for managing UniFi networks via Site Manager API"
    homepage: "https://github.com/dl-alexandre/UniFi-Site-Manager-CLI"
    license: MIT
    skip_upload: "{{ if index .Env \"SCOOP_BUCKET_TOKEN\" }}false{{ else }}true{{ end }}"
    persist:
      - "config.yaml"

# dockers:
#   - image_templates:
#       - "ghcr.io/dl-alexandre/usm:{{ .Tag }}"
#       - "ghcr.io/dl-alexandre/usm:v{{ .Major }}"
#       - "ghcr.io/dl-alexandre/usm:v{{ .Major }}.{{ .Minor }}"
#       - "ghcr.io/dl-alexandre/usm:latest"
#     dockerfile: Dockerfile
#     build_flag_templates:
#       - "--pull"
#       - "--label=org.opencontainers.image.created={{.Date}}"
#       - "--label=org.opencontainers.image.title={{.ProjectName}}"
#       - "--label=org.opencontainers.image.revision={{.FullCommit}}"
#       - "--label=org.opencontainers.image.version={{.Version}}"

# announce:
#   twitter:
#     enabled: true
#     message_template: "UniFi Site Manager CLI {{.Tag}} is out! Check it out: https://github.com/dl-alexandre/UniFi-Site-Manager-CLI/releases/{{.Tag}}"
